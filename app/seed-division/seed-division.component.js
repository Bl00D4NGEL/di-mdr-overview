'use strict';

// Register `seedDivision` component, along with its associated controller and template
angular.module('seedDivision').component('seedDivision', {
    templateUrl: 'seed-division/seed-division.template.html',
});